  <h1>Our Products!</h1><table cellspacing="10" layout="fixed">
  <tr>
    <th></th>
    <th>Name</th>
    <th>Description</th>
    <th>Price</th>
    <th></th>
    <th></th>
  </tr>
  <% @products.each do |product| %>
    <tr>
      <td><img src="<%= product.image_url %>" alt="<%= product.name %>"></td>
      <td><%= link_to product.name, product_path(product) %></td>
      <td><%= product.description %></td>
      <td><%= number_to_currency(product.price) %></td>
      <td><%= button_to 'Add to Cart', cart_products_path(product_id: product) %></td>
      <% if @user.is_seller == true && product.seller == @user  %>
      <td><%= link_to 'Edit', edit_product_path(product) %></td>
      <td><%= link_to 'Delete', product_path(product), method: :delete %></td>
      <% end %>

    </tr>
  <% end %>
</table>
